services:
  mongo:
    image: mongo:7
    container_name: freelancehub-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: freelancehub

volumes:
  mongo_data:
